import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AUChangePassword = () => {
  return (
    <View>
      <Text>AUChangePassword</Text>
    </View>
  );
};

export default AUChangePassword;

const styles = StyleSheet.create({});
